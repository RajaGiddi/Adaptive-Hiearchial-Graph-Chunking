# Demo Document

This is a small demo document used by the pipeline test.

- It contains a few lines of text.
- The ingestion pipeline will segment, summarize, group, and build a graph.
- Content is intentionally simple to keep tests fast and deterministic.
